---
description: Run the Phase 1 multi-agent development workflow for FaustusControllerLite
agent: orchestrator
---

Implement the following request using the FaustusControllerLite engineering workflow:

$ARGUMENTS

Begin by inspecting the repository, CLAUDE.md, AGENTS.md, the relevant source, and related tests. Classify the task before delegating. Keep changes scoped to the plugin source and its related tests. Do not edit runtime data or compiled host binaries.

At completion, summarize:

- implementation
- files changed
- tests and build checks
- independent review
- remaining risks
